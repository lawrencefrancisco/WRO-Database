# General Rules

- **Version Control**: Do not automatically run `git` commands (e.g., `git add`, `git commit`, `git push`, `git pull`, `git rebase`) on behalf of the user unless explicitly instructed. Make code changes locally and leave version control and deployment to the user.
